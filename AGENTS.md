# Undead Tower 开发约定

- 本项目为独立仓库，改动限制在项目目录内。
- M0 的固定朝向是核心约束：不能引入累积鼠标转向、自由移动或 Pointer Lock FPS 控制器，除非用户明确要求改变规则。
- 瞄准和弹道必须共享目标点，开火时从枪口检测遮挡；不要只旋转装饰枪模。
- 场景为原创低多边形风格，不导入《未转变者》的模型或贴图。
- Windows PowerShell 读取中文源文件必须使用 `Get-Content -Encoding utf8`。
- Git 提交格式为 `<type>: <简体中文具体摘要>`，类型使用英文小写，末尾不加句号。每次提交只含一项逻辑相关改动。
- 存在远程仓库时，必要验证通过后提交并推送本次相关文件；不包含用户无关改动，不强制推送或改写历史。没有远程或推送受阻时说明原因。
- 浏览器验收保持单 worker，不同时运行多份 3D 游戏测试；标题页与暂停页必须按需绘制，后台停止绘制。
- 正式模式的刷新率最多每秒 10 只；移动和刷新使用有效游玩时间，暂停与后台不能增加坚守成绩。
- 练习模式的僵尸保持静止、击倒复位；只有正式模式自然失败才写入对应难度的本机排行榜。
- 尸群使用实例化绘制并限制同时存在的数量；不要为每只僵尸创建独立材质和场景节点树。
